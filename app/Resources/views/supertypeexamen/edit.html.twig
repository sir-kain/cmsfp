{% extends '::base.html.twig' %}

{% block header %}
    {% include 'FPCMSBundle:layout:header.html.twig' %}
{% endblock %}
{% block body %}

    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Modification type examen</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-6 col-lg-offset-3">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        {#Modifier ce type d'examen#}
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-lg-10 col-lg-offset-1">
                                {{ form_start(form) }}
                                {{ form_widget(form) }}
                                <input type="submit" class="btn btn-outline btn-success" value="Modifier" />
                                <a class="btn btn-bitbucket" href="{{ path('supertypeexamen_index') }}">
                                    Retour à la liste</a>
                                {{ form_end(form) }}
                                {{ form_start(delete_form) }}
                                <input type="submit" class="btn btn-outline btn-danger pull-right" value="Supprimer" >
                                {{ form_end(delete_form) }}
                            </div>
                            <!-- /.col-lg-6 (nested) -->
                        </div>
                        <!-- /.row (nested) -->
                    </div>
                    <!-- /.panel-body -->
                </div>


                <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{% endblock %}
